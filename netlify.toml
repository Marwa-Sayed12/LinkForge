[build]
  command = "npm run build"
  publish = "dist"

# Redirect all short links to the Netlify Function
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/redirect/:splat"
  status = 200
  force = true